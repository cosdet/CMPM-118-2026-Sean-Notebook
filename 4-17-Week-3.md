# Week 3
### What I Did

I called with Daniel this week! It was a quick 30 minute call, but I informed him of my goals to implement a terminal text editor and he shared his current progress on developing KiBi. It showed me that a lot of the Twizzler syscalls I'm working with are translated from the form `twz_rt_fd_get_config` to `fd_get_config` in `src/rt/reference/src/runtime/file.rs`, so I've been taking a look at that and seeing if I can get rid of some of the `todo!()`s that are still present in those functions. Unfortunately, it seems the changes I need require changing the `twizzler-abi` submodule, which causes the build hash to refresh, which, in turn, causes Twizzler not to build unless a new toolchain is pulled, which I don't have the resources for :( So I'm talking with Daniel to possibly schedule another meeting to fix these things or just make a new toolchain once he merges his changes.

I also finished up my RFC for the terminal editor and defined my Week 6 and Week 10 goals.

### Troubles

I spent a long time tracing where exactly `femto` was failing when I was running it. It was weirdly calling `tcgetattr` with `1` as it's file descriptor number, however, by the time it reaches Twizzler's `fd_get_conifg` (which is the equivalent function) the file descriptor number is somehow `3`?? Even DeepWiki couldn't figure out why this happens and I've lost the motivation to search through the whole repository chasing function definitions for now. Anyway, this is a problem because there currently isn't a case in Twizzler's runtime for a file descriptor of the number `3`, as there are only definitions for `0`, `1`, and `2`. However, even those defined file descriptor options all lead to `KernelConsole` and not `Pty` as they probably should? It's very confusing. Daniel's `dbittman-e1000-driver` branch has changes to the runtime, kernel, and abi that I think update these definitions to work and have better handling for undefined file descriptor numbers, but attempting to cherry-pick these changes has proven to be unsuccessful as there always seems to be something missing or undefined that would be found in another file. I think I could solve all the inter-dependencies and get the changes Daniel's made if I spent a few hours adding the feature then building and seeing what's missing, but I think I'll just meet with him first to discuss the best course of action.

### Goals and Aspirations

My goal is to meet with Daniel and talk about the weird runtime stuff!
And then try and get `femto` to run once I get Daniel's changes implented on my end (or at least pseudo-implemented)
